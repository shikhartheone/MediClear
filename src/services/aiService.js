export const getSimplifiedReport = async (rawText) => {
  console.log("--- AI Service Called (Placeholder) ---");
  return {
    message: "AI service placeholder: simplified report would be here.",
    originalText: rawText,
  };
};
